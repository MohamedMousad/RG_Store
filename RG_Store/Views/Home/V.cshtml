@* @model IEnumerable<Pro.ViewModel.CreateOrder>
@{
    ViewData["Tittle"] = "User View";
}
<!DOCTYPE html>
    <html>
    <head>
        <title></title>
    </head>
    <body>
        <h1>User View</h1U>
    <hr />
    @if(Where(x=>x.username == ))
    {
    @if (Model == null || !Model.Any())
    {
        <p>No orders available.</p>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>User Name</th>
                    <th>User Address</th>
                    <th>User Phone</th>
                    <th>User Gender</th>
                    <th>Order Date</th>
                    <th>Order Cost</th>
                    <th>Delivery Date</th>
                    <th>Item ID</th>
                    <th>Item Name</th>
                    <th>Item Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model)
                {
                    <tr>
                        <td>@order.userid</td>
                        <td>@order.username</td>
                        <td>@order.useraddress</td>
                        <td>@order.userphone</td>
                        <td>@order.usergender</td>
                        <td>@order.orederDate</td>
                        <td>@order.orderCost</td>
                        <td>@order.orderDeliveryDate</td>
                        <td>@order.itemid</td>
                        <td>@order.itemName</td>
                        <td>@order.itemPrice</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    }
    </body>
    </html> *@